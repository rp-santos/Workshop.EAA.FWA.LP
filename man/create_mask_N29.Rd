% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_mask_N29.R
\name{create_mask_N29}
\alias{create_mask_N29}
\title{Create a habitat mask in UTM zone 29N}
\usage{
create_mask_N29(
  x_range,
  y_range,
  utm_resolution = 1000,
  x_pad_perc = 0.05,
  y_pad_perc = 0.05,
  buffer = 12000,
  exclude_regions = NULL
)
}
\arguments{
\item{x_range}{Numeric vector of length 2 defining the minimum
and maximum x coordinates (UTM eastings) of the study area.}

\item{y_range}{Numeric vector of length 2 defining the minimum
and maximum y coordinates (UTM northings) of the study area.}

\item{utm_resolution}{Numeric value indicating the spacing between
mask points in meters. Defaults to \code{1000}.}

\item{x_pad_perc}{Numeric value indicating the percentage of
padding added to the x-axis extent. Defaults to \code{0.05}.}

\item{y_pad_perc}{Numeric value indicating the percentage of
padding added to the y-axis extent. Defaults to \code{0.05}.}

\item{buffer}{Numeric value indicating the buffer distance (in
meters) added around the trapping grid. Defaults to \code{12000}.}

\item{exclude_regions}{Optional spatial polygon object defining
areas to exclude from the habitat mask (e.g. land areas).
Defaults to \code{NULL}.}
}
\value{
An object of class \code{mask} generated with \code{\link[secr:make.mask]{secr::make.mask()}},
suitable for use in SECR and openCR analyses.
}
\description{
\code{\link[=create_mask_N29]{create_mask_N29()}} generates a spatial habitat mask for
capture-recapture analyses using UTM zone 29N coordinates
(EPSG:32629).

The mask is created from a regular UTM grid generated with
\code{\link[=create_utm_grid_N29]{create_utm_grid_N29()}} and converted into a habitat mask using
\code{\link[secr:make.mask]{secr::make.mask()}}. The resulting object can be used in spatially
explicit capture-recapture (SECR) and open population analyses.

Optional exclusion polygons can be provided to remove unsuitable
habitat areas (e.g. land masses).
}
\details{
The function performs the following steps:
\enumerate{
\item Creates a regular UTM grid using
\code{\link[=create_utm_grid_N29]{create_utm_grid_N29()}}
\item Applies a half-cell offset so mask points are centered relative
to detector locations
\item Generates a habitat mask using \code{\link[secr:make.mask]{secr::make.mask()}}
\item Optionally excludes regions defined in \code{exclude_regions}
}

The resulting mask defines the spatial state space used for
estimating animal density and movement parameters.
}
\examples{
\dontrun{

habitat_mask <- create_mask_N29(
  x_range = c(200000, 300000),
  y_range = c(4100000, 4200000)
)

plot(habitat_mask)

}

}
