% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/whale_prob_alg.R
\name{whale_prob_alg}
\alias{whale_prob_alg}
\title{Compute spatial probability surface for whale detections}
\usage{
whale_prob_alg(mask, grid, capture, params, detectfn = hex)
}
\arguments{
\item{mask}{A matrix or data frame of spatial locations (e.g. habitat
mask or grid points). The first two columns must correspond to
easting and northing coordinates.}

\item{grid}{A raster object representing the spatial grid used for
extracting detector coordinates.}

\item{capture}{A data frame containing capture history information.
Must include a column named \code{Detector} indicating the assigned grid
cell index for each observation.}

\item{params}{A named list containing detection parameters:
\describe{
\item{sigma}{Spatial scale parameter of the detection function}
\item{lambda0}{Baseline detection parameter}
}}

\item{detectfn}{A detection function used to convert distances into
probabilities. Defaults to \code{\link[=hex]{hex()}}.}
}
\value{
A tibble containing the original mask coordinates and a column
\code{p}, representing the normalised spatial probability of detection.
}
\description{
\code{\link[=whale_prob_alg]{whale_prob_alg()}} estimates a spatial probability surface for whale
detections by combining a detection function with distances between
observed locations and a spatial mask/grid.

The function accumulates detection probabilities across all observed
individuals and normalises them to produce a relative spatial
probability surface.
}
\details{
The function:
\enumerate{
\item Extracts detector coordinates from the raster grid
\item Converts them to UTM coordinates using \code{\link[=longlat_to_utm_alg]{longlat_to_utm_alg()}}
\item Computes distances between each capture and all mask points using
\code{\link[=whale_distances]{whale_distances()}}
\item Applies the detection function to convert distances into
probabilities
\item Aggregates probabilities across all captures
\item Normalises the resulting surface to sum to 1
}
}
\examples{
\dontrun{

surf <- whale_prob_alg(
  mask = mask,
  grid = grid,
  capture = capture,
  params = list(sigma = 3000, lambda0 = 0.2)
)

head(surf)

}

}
