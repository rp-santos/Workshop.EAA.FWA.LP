% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/whale_distances.R
\name{whale_distances}
\alias{whale_distances}
\title{Compute distances between a whale location and mask points}
\usage{
whale_distances(i, mask, grid_tbl, capture)
}
\arguments{
\item{i}{Integer index identifying the focal row in the capture
history.}

\item{mask}{A matrix or object containing spatial coordinates of
detector locations. The first two columns must correspond to
x (easting) and y (northing) coordinates.}

\item{grid_tbl}{A data frame or tibble containing detector
information, including:
\describe{
\item{easting}{X coordinate of detector locations}
\item{northing}{Y coordinate of detector locations}
}}

\item{capture}{A data frame containing capture information with at
least a column named \code{Detector} indicating the assigned detector
index for each observation.}
}
\value{
A numeric vector of Euclidean distances between the focal whale
location and all points in \code{mask}.
}
\description{
\code{\link[=whale_distances]{whale_distances()}} calculates the Euclidean distance between a
focal whale location (identified through a capture history) and all
spatial points in a mask/grid.

This function is typically used in spatial capture-recapture (SECR)
workflows to compute detection distances between an individual
location and detector grid cells.
}
\details{
The function:
\enumerate{
\item Extracts the detector index associated with observation \code{i}
\item Retrieves the corresponding easting and northing coordinates
\item Computes Euclidean distances to all mask/grid points
\item Returns a numeric vector of distances
}
}
\examples{
\dontrun{

d <- whale_distances(
  i = 1,
  mask = mask_matrix,
  grid_tbl = grid,
  capture = capture_history
)

head(d)

}

}
