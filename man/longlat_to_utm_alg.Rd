% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/longlat_to_utm_alg.R
\name{longlat_to_utm_alg}
\alias{longlat_to_utm_alg}
\title{Convert longitude/latitude coordinates to UTM}
\usage{
longlat_to_utm_alg(longlat, crs_from = 4326, crs_to = 32629)
}
\arguments{
\item{longlat}{A data frame or tibble containing geographic
coordinates.

Must include the columns:

\describe{
\item{lon}{Longitude in decimal degrees (WGS84)}
\item{lat}{Latitude in decimal degrees (WGS84)}
}}

\item{crs_from}{Numeric EPSG code of the input coordinate reference
system. Defaults to \code{4326} (WGS84).}

\item{crs_to}{Numeric EPSG code of the output coordinate reference
system. Defaults to \code{32629} (UTM zone 29N).}
}
\value{
A tibble with UTM coordinates:

\describe{
\item{easting}{Projected x-coordinate in meters}
\item{northing}{Projected y-coordinate in meters}
}
}
\description{
\code{\link[=longlat_to_utm_alg]{longlat_to_utm_alg()}} converts geographic coordinates (longitude
and latitude) into projected UTM coordinates using the
\code{sf} package.

This function is designed for spatial analyses in the Algarve /
eastern Atlantic region, using UTM zone 29N (EPSG:32629) by default.
}
\details{
The function performs the following steps:
\enumerate{
\item Checks that required columns (\code{lon}, \code{lat}) are present
\item Converts the input data frame into an \code{sf} POINT object
using the specified input CRS
\item Transforms coordinates into UTM zone 29N (or user-defined CRS)
\item Extracts projected coordinates and returns them as a tibble
}
}
\examples{
\dontrun{

coords <- data.frame(
  lon = c(-8.9, -8.7),
  lat = c(37.0, 37.2)
)

utm <- longlat_to_utm_alg(coords)

head(utm)

}

}
