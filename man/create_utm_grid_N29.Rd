% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_utm_grid_N29.R
\name{create_utm_grid_N29}
\alias{create_utm_grid_N29}
\title{Create a regular UTM grid in zone 29N}
\usage{
create_utm_grid_N29(
  x_range,
  y_range,
  utm_resolution = 1000,
  x_pad_perc = 0.05,
  y_pad_perc = 0.05,
  x_offset = -utm_resolution/2,
  y_offset = -utm_resolution/2
)
}
\arguments{
\item{x_range}{Numeric vector of length 2 defining the minimum
and maximum longitude values of the study area.}

\item{y_range}{Numeric vector of length 2 defining the minimum
and maximum latitude values of the study area.}

\item{utm_resolution}{Numeric value indicating the spacing between
grid points in meters. Defaults to \code{1000}.}

\item{x_pad_perc}{Numeric value indicating the percentage of
padding added to the longitude range. Defaults to \code{0.05}.}

\item{y_pad_perc}{Numeric value indicating the percentage of
padding added to the latitude range. Defaults to \code{0.05}.}

\item{x_offset}{Numeric value defining an offset applied to the
x coordinates (eastings) of the grid. Defaults to
\code{-utm_resolution/2}.}

\item{y_offset}{Numeric value defining an offset applied to the
y coordinates (northings) of the grid. Defaults to
\code{-utm_resolution/2}.}
}
\value{
A data frame containing UTM grid coordinates with columns:

\describe{
\item{x}{UTM easting coordinate}
\item{y}{UTM northing coordinate}
}

The object also contains attributes:

\describe{
\item{nx}{Number of grid cells along the x-axis}
\item{ny}{Number of grid cells along the y-axis}
}
}
\description{
\code{\link[=create_utm_grid_N29]{create_utm_grid_N29()}} generates a regular spatial grid in
UTM zone 29N (EPSG:32629) based on longitude and latitude ranges.

The function converts geographic coordinates (WGS84) into UTM
coordinates and creates a regularly spaced grid using
\code{\link[secr:make.traps]{secr::make.grid()}}.

Optional padding can be applied to expand the study area extent,
and offsets can be used to shift grid cell locations relative to
detector positions or habitat masks.
}
\details{
The function performs the following steps:
\enumerate{
\item Expands the geographic extent using padding percentages
\item Converts longitude/latitude coordinates to UTM zone 29N using
\code{\link[azores.fkw:longlat_to_utm]{azores.fkw::longlat_to_utm()}}
\item Computes the number of grid cells required along each axis
\item Generates a regular UTM grid using \code{\link[secr:make.traps]{secr::make.grid()}}
\item Applies optional coordinate offsets to shift grid locations
}

The resulting grid can be used to create detector arrays,
habitat masks, or spatial rasters for SECR analyses.
}
\examples{
\dontrun{

utm_grid <- create_utm_grid_N29(
  x_range = c(-9.5, -8.5),
  y_range = c(36.5, 37.5)
)

head(utm_grid)

}

}
